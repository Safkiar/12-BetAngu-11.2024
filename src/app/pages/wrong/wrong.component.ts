import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-wrong',
  standalone: true,
  imports: [RouterModule],
  templateUrl: './wrong.component.html',
  styleUrl: './wrong.component.scss',
})
export class WrongComponent {}
